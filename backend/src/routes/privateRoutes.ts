import { Router } from "express";

const router = Router();

// ❌ RUTA ELIMINADA: Endpoint duplicado e innecesario
// Los mensajes privados se manejan exclusivamente por WebSocket
// Esta ruta estaba duplicada y no proporcionaba funcionalidad real

export default router;